# Copie para .env.local e preencha. .env.local NÃO é versionado.

# Supabase (opcional) — sem isto o site funciona em modo demo via RSS direto
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Email registrado no MyMemory — aumenta o limite de tradução (opcional)
MYMEMORY_EMAIL=
